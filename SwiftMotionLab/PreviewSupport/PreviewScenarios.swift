import SwiftUI

// The app's canonical named previews, built from the named scenarios in `PreviewData.swift`.

#Preview("Linear · the boring baseline") {
    ContentView(PreviewData.linearBaseline)
}
